---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

// Mock data - will be replaced with real data from R2/database
const influencers = [
  {
    id: '1',
    name: 'Pandawara Group',
    handle: '@pandawaragroup',
    platform: 'tiktok',
    avatarUrl: '/avatars/default.png',
    tier: 'gold',
    impactScore: 95000,
    totalImpressions: 10500000,
  },
  {
    id: '2',
    name: 'Sungai Watch',
    handle: '@sungaiwatch',
    platform: 'instagram',
    avatarUrl: '/avatars/default.png',
    tier: 'gold',
    impactScore: 78000,
    totalImpressions: 5200000,
  },
  {
    id: '3',
    name: 'Zero Waste ID',
    handle: '@zerowasteid',
    platform: 'instagram',
    avatarUrl: '/avatars/default.png',
    tier: 'silver',
    impactScore: 45000,
    totalImpressions: 1800000,
  },
];

function formatNumber(num: number): string {
  if (num >= 1000000) {
    return (num / 1000000).toFixed(1) + 'M';
  }
  if (num >= 1000) {
    return (num / 1000).toFixed(1) + 'K';
  }
  return num.toLocaleString('id-ID');
}

function getTierBadge(tier: string) {
  switch (tier) {
    case 'platinum':
      return { emoji: '💎', label: 'Champion Nasional', bg: 'bg-purple-100', text: 'text-purple-800' };
    case 'gold':
      return { emoji: '🥇', label: 'Duta Buyback', bg: 'bg-yellow-100', text: 'text-yellow-800' };
    case 'silver':
      return { emoji: '🥈', label: 'Pejuang Lingkungan', bg: 'bg-gray-100', text: 'text-gray-800' };
    default:
      return { emoji: '🥉', label: 'Pendukung', bg: 'bg-orange-100', text: 'text-orange-800' };
  }
}

function getPlatformIcon(platform: string) {
  switch (platform) {
    case 'instagram':
      return '📸';
    case 'tiktok':
      return '🎵';
    case 'twitter':
      return '𝕏';
    case 'youtube':
      return '▶️';
    default:
      return '🌐';
  }
}
---

<BaseLayout 
  title="Leaderboard" 
  description="Lihat influencer dan pendukung yang paling berpengaruh dalam gerakan buyback kemasan plastik Indonesia."
>
  <Header />
  
  <main>
    <!-- Hero -->
    <section class="section bg-gradient-to-br from-accent-50 to-white">
      <div class="container-wide">
        <div class="text-center mb-12">
          <h1 class="font-display text-4xl md:text-5xl font-bold text-gray-900 mb-4">
            Leaderboard
          </h1>
          <p class="text-lg text-gray-600 max-w-2xl mx-auto">
            Influencer dan pendukung yang memberikan dampak terbesar untuk gerakan buyback Indonesia.
          </p>
        </div>
        
        <!-- Filter -->
        <div class="flex flex-wrap justify-center gap-2 mb-8" x-data="{ filter: 'all' }">
          <button 
            class="px-4 py-2 rounded-full text-sm font-medium transition-colors"
            :class="filter === 'all' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200'"
            @click="filter = 'all'"
          >
            Semua
          </button>
          <button 
            class="px-4 py-2 rounded-full text-sm font-medium transition-colors"
            :class="filter === 'instagram' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200'"
            @click="filter = 'instagram'"
          >
            📸 Instagram
          </button>
          <button 
            class="px-4 py-2 rounded-full text-sm font-medium transition-colors"
            :class="filter === 'tiktok' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200'"
            @click="filter = 'tiktok'"
          >
            🎵 TikTok
          </button>
          <button 
            class="px-4 py-2 rounded-full text-sm font-medium transition-colors"
            :class="filter === 'twitter' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200'"
            @click="filter = 'twitter'"
          >
            𝕏 Twitter
          </button>
          <button 
            class="px-4 py-2 rounded-full text-sm font-medium transition-colors"
            :class="filter === 'youtube' ? 'bg-primary-600 text-white' : 'bg-gray-100 text-gray-700 hover:bg-gray-200'"
            @click="filter = 'youtube'"
          >
            ▶️ YouTube
          </button>
        </div>
      </div>
    </section>
    
    <!-- Leaderboard List -->
    <section class="py-8 bg-white">
      <div class="container-wide">
        <div class="space-y-4">
          {influencers.map((influencer, index) => {
            const tier = getTierBadge(influencer.tier);
            const platform = getPlatformIcon(influencer.platform);
            
            return (
              <div class="card flex flex-col md:flex-row md:items-center gap-4 hover:shadow-xl transition-shadow">
                <!-- Rank -->
                <div class="flex items-center justify-center w-12 h-12 rounded-full bg-primary-100 text-primary-700 font-display font-bold text-xl shrink-0">
                  {index + 1}
                </div>
                
                <!-- Avatar & Info -->
                <div class="flex items-center gap-4 flex-1">
                  <div class="w-14 h-14 rounded-full bg-gray-200 flex items-center justify-center text-2xl">
                    {platform}
                  </div>
                  <div>
                    <div class="flex items-center gap-2 flex-wrap">
                      <h3 class="font-semibold text-lg">{influencer.name}</h3>
                      <span class={`badge ${tier.bg} ${tier.text}`}>
                        {tier.emoji} {tier.label}
                      </span>
                    </div>
                    <p class="text-gray-500 text-sm">{influencer.handle}</p>
                  </div>
                </div>
                
                <!-- Stats -->
                <div class="flex items-center gap-6 md:gap-8">
                  <div class="text-center">
                    <div class="font-display font-bold text-xl text-accent-600">
                      {formatNumber(influencer.totalImpressions)}
                    </div>
                    <div class="text-xs text-gray-500">Impressions</div>
                  </div>
                  <div class="text-center">
                    <div class="font-display font-bold text-xl text-primary-600">
                      {formatNumber(influencer.impactScore)}
                    </div>
                    <div class="text-xs text-gray-500">Impact Score</div>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
        
        <!-- Empty State -->
        {influencers.length === 0 && (
          <div class="text-center py-12">
            <div class="text-5xl mb-4">🏆</div>
            <h3 class="font-semibold text-lg mb-2">Belum Ada Data</h3>
            <p class="text-gray-600">
              Leaderboard akan diperbarui setelah campaign dimulai.
            </p>
          </div>
        )}
        
        <!-- Join CTA -->
        <div class="mt-12 text-center">
          <div class="card bg-primary-50 border-primary-100 max-w-2xl mx-auto">
            <h3 class="font-display font-bold text-xl text-primary-800 mb-3">
              Ingin Masuk Leaderboard?
            </h3>
            <p class="text-primary-700 mb-4">
              Daftar sebagai influencer pendukung dan bantu sebarkan gerakan buyback.
            </p>
            <a href="/bergabung" class="btn btn-primary">
              Daftar Sebagai Influencer
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>
  
  <Footer />
</BaseLayout>
